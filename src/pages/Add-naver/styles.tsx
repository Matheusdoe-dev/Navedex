import styled from "styled-components";

export const AddNaverBody = styled.div`
  & {
    min-height: 100vh;

    &.modal {
      background: var(--gray-900);
      filter: brightness(50%);
    }
  }
`;
